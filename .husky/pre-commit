#!/usr/bin/env sh

echo "================================================="
echo "🚀 ZERO-VAULT: KIỂM TRA CHẤT LƯỢNG TRƯỚC KHI COMMIT"
echo "================================================="

echo "\n[1/3] 🧹 Đang chạy ESLint & Prettier..."
npx lint-staged || exit 1

echo "\n[2/3] 📘 Đang kiểm tra Type (TypeScript)..."
# Ép TypeScript kiểm tra toàn bộ project xem có lỗi type (báo đỏ) nào không
npx tsc --noEmit || exit 1

echo "\n[3/3] 🧪 Đang chạy Unit & Integration Tests..."
# Lưu ý quan trọng: Phải dùng 'vitest run' để test chạy 1 lần rồi dừng. 
# Nếu dùng chữ 'vitest' không, nó sẽ mở chế độ Watch Mode và làm treo Terminal!
npx vitest run || exit 1

echo "\n✅ TẤT CẢ ĐỀU XANH MƯỢT! COMMIT THÀNH CÔNG!"
echo "=================================================\n"